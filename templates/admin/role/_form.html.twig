<h1>form role</h1>

<div class="container">
    <div class="row justify-content-center">
        {{ form_start(form, {'attr': {'class': 'col-sm-12 col-md-8'}}) }}
            {{ form_widget(form) }}
        <button class="btn btn-primary px-5 d-block mx-auto font-weight-bold" type="submit">
            {% if app.request.attributes.get('_route') == 'admin_role' %}
                Créer
            {% else %}
                Modifier
            {% endif %}
        </button>
        {{ form_end(form) }}
    </div>
</div>