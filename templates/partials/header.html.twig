<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <a class="navbar-brand" href="#">Site symfony test !</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('afficher_annonces') }}">Annonces</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('creer_annonce') }}">Créer une annonce</a>
            </li>
        </ul>

        {% if not app.user %}
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="#" class="nav-link">Inscription</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('connexion_compte_utilisateur') }}" class="nav-link">Connexion</a>
                </li>
            </ul>
        {% else %}
            <div class="dropdown show">
                <a class="dropdown-toggle" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img class="profil-image" src="{{ app.user.avatar }}" alt="">
                </a>
            
                <div class="dropdown-menu bg-light border-0" aria-labelledby="dropdownMenuLink">
                    <a class="dropdown-item text-center" href="{{ path('afficher_utilisateur', {'slug': app.user.slug }) }}">Mon profile</a>
                    <a class="dropdown-item text-center" href="{{ path('deconnexion_compte_utilisateur') }}">Déconnexion</a>
                </div>
            </div>
        {% endif %}
    </div>
</nav>